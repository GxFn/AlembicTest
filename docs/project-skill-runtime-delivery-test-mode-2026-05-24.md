# Project Skill Runtime Delivery Test Mode Retest - 2026-05-24

## Window And Scope

- Test case: `Test-2026-05-24-05 / CSSD-P4B-TestMode-Test`
- Execution window: `AlembicTest`
- Real test project: `BiliDili`
- Scope: test-mode cold-start with one dimension and small sample, plus Plugin MCP project-skill delivery probe.
- Non-goals: no BiliDili product source changes, no full cold-start gate, no Alembic / AlembicPlugin product fixes in this repo.

## Conclusion

Result: pass with follow-up observations.

- Test mode was enabled and constrained to `architecture` only.
- The daemon bootstrap completed with `1` dimension, `8` files, `5` candidates, `1` generated skill, and `1` Alembic route `ProjectSkillDeliveryReceipt`.
- The Plugin route fully passed: `alembic_project_skill` created a Plugin receipt, exported by `symlink-first` into BiliDili `.agents/skills`, wrote a matching managed marker, loaded from `codex-runtime`, blocked a non-managed conflict, and cleaned BiliDili back to a clean git state.
- Alembic route generated `project-architecture` and a receipt, but runtime export stayed `pending` by design: Alembic generated the receipt, and Plugin authorization/export is the required follow-up route.
- REST process events initially lagged behind live daemon logs during execution, but after job completion `/api/v1/jobs/:jobId/events` contained `llm.input`, `tool`, `llm.output`, `llm.reflection`, summaries, and artifact entries.

## Configuration

- Daemon URL: `http://127.0.0.1:50566`
- Dashboard URL: `http://127.0.0.1:50566/jobs?job=bootstrap_mpjkpvdy_c12ba098`
- Environment:
  - `ALEMBIC_TEST_MODE=1`
  - `ALEMBIC_TEST_BOOTSTRAP_DIMS=architecture`
  - `ALEMBIC_TEST_RESCAN_DIMS=architecture`
- Bootstrap request:
  - `maxFiles=8`
  - `contentMaxLines=40`
  - `skipGuard=true`
- Test mode endpoint:
  - `enabled=true`
  - `bootstrapDims=["architecture"]`
  - `rescanDims=["architecture"]`
  - terminal test mode enabled with `terminal-run`
  - sandbox mode `enforce`

## Version Evidence

- `AlembicPlugin` HEAD: `3e422ab9ed081783d3273b82d6c3beacd61ee2e7`
- `AlembicPlugin/plugins/alembic-codex/runtime` HEAD: `eff3ea7ead8773e8315a923e7d4ea57281242f01`
- Alembic daemon/package version from health: `0.2.0`
- BiliDili git status before and after probe cleanup: clean.

## Job Evidence

- Job id: `bootstrap_mpjkpvdy_c12ba098`
- Session id: `bs_1779614825558_snyz66`
- Status: `completed`
- Duration: `417` seconds
- Project files sampled: `8`
- Dimensions: `architecture`
- Totals:
  - candidates: `5`
  - skills: `1`
  - tool calls: `34`
  - errors: `0`
  - Project Skill delivery receipts: `1`
- Generated Alembic skill: `${HOME}/.asd/workspaces/02a25032/Alembic/skills/project-architecture/SKILL.md`

## Events API

Evidence file: `AlembicTest/tmp/cssd-p4b-testmode-events-bootstrap_mpjkpvdy_c12ba098.json`

Final event counts:

| kind | count |
| --- | ---: |
| `workflow` | 5 |
| `checkpoint` | 1 |
| `llm.input` | 1 |
| `tool` | 1 |
| `llm.output` | 1 |
| `llm.reflection` | 1 |
| `summary` | 3 |
| `artifact` | 1 |

Notable event evidence:

- `llm.input`: developer-facing JSON summary, `rawProviderPayload=false`, prompt expansion and secrets omitted.
- `tool`: `34` tool calls recorded; tool names include `code`, `graph`, `note_finding`, `knowledge`, and `meta`.
- `llm.output`: visible assistant markdown captured for the architecture dimension.
- `llm.reflection`: quality-gate self-check passed.
- `hiddenCount=0`.

Observation: while the job was still running, REST events stayed at 7 entries through `llm.input`, while daemon logs already showed real `llm.output`, `knowledge`, and `note_finding` activity. REST recovered after completion and returned 14 retained events. This is not a failure for final retained evidence, but it is a live append/recovery observation.

## Alembic Route Receipt

Evidence file: `AlembicTest/tmp/cssd-p4b-testmode-report-latest-bootstrap_mpjkpvdy_c12ba098.json`

- `projectSkillDelivery.contract=ProjectSkillDeliveryReceipt`
- `route=alembic`
- `receiptCount=1`
- `skillName=project-architecture`
- `receiptId=alembic-skill-02a25032-project-architecture-5baeb1ec18754d92`
- `asset.path=${HOME}/.asd/workspaces/02a25032/Alembic/skills/project-architecture/SKILL.md`
- `runtimeExport.status=pending`
- `runtimeExport.authorizationStatus=pending`
- `runtimeExport.strategy=symlink-first`
- `runtimeExport.targetPath=BiliDili/.agents/skills/project-architecture`
- `shoutSummary.message=Project Skill project-architecture generated by Alembic; Codex runtime export is pending.`

Interpretation: Alembic route correctly generated a minimal receipt and shout summary. Runtime export is intentionally pending because project-level Codex skill root authorization belongs to the Plugin route.

## Plugin MCP Route

Evidence file: `AlembicTest/tmp/project-skill-delivery-probe-2026-05-24T09-28-08-585Z.json`

Probe result: `ok=true`.

MCP `tools/list` evidence:

- `alembic_project_skill=true`
- `alembic_skill=true`
- The probe used `alembic_project_skill` for runtime delivery. `alembic_skill` remained a legacy compatibility surface and was not used for Codex runtime export.

Probe checks:

- `mcpToolVisible=true`
- `pluginRouteReceipt=true`
- `runtimeExported=true`
- `runtimeSymlink=true`
- `markerMatchesReceipt=true`
- `runtimeLoadUsesCodexRuntime=true`
- `conflictBlocked=true`
- `conflictFilePreserved=true`
- `gitOnlyExpectedRuntimeExport=true`
- `noGlobalHomeSkillWrites=true`
- `testModeRequested=true`

Plugin receipt summary:

- `route=plugin`
- `runtimeExport.status=exported`
- `runtimeExport.authorizationStatus=granted`
- `runtimeExport.conflictStatus=target-missing`
- `runtimeExport.targetPath=BiliDili/.agents/skills/alembic-test-runtime-delivery-20260524092808`

## Codex Project Skill Discovery And Use

Discovery:

- `alembic_project_skill list` reported `codexRuntime.root=BiliDili/.agents/skills`.
- During the probe, `codexRuntime.exports` contained `alembic-test-runtime-delivery-20260524092808` with `visible=true` and `managed=true`.

Use:

- `alembic_project_skill load` returned `success=true`.
- `load` source was `codex-runtime`.
- Loaded content contained the probe sentinel, proving Codex project runtime content was read from the symlinked `SKILL.md`.

Current Codex sessions do not hot reload the system-provided skill list mid-thread; this test proves runtime visibility through the Plugin MCP project skill surface and filesystem contract.

## Symlink, Marker, Conflict, And Git

Symlink / marker:

- Runtime `SKILL.md` existed under `BiliDili/.agents/skills/alembic-test-runtime-delivery-20260524092808/SKILL.md`.
- `SKILL.md` was a symlink to the Alembic-managed storage file under `${HOME}/.asd/workspaces/02a25032/Alembic/skills/...`.
- `.alembic-managed.json` existed.
- Marker matched receipt fields: `generatedSkillId`, `generationHash`, `projectScopeId`, `route`, `skillName`, and `sourcePath`.

Conflict:

- Probe pre-created a non-managed `BiliDili/.agents/skills/alembic-test-conflict-20260524092808/SKILL.md`.
- Plugin export returned `conflictStatus=different-existing` and `runtimeExport.status=blocked`.
- The non-managed file stayed non-symlink and preserved the sentinel text.

Git and global boundary:

- BiliDili git before probe: clean.
- During runtime export: `?? .agents/` only, expected for the temporary project runtime export.
- After cleanup: clean.
- No `$HOME/.agents/skills/<probe-skill>` writes were detected.
- BiliDili `.agents/skills` directory remains empty; empty directories are not tracked by git.

## Commands

```bash
ALEMBIC_TEST_MODE=1 ALEMBIC_TEST_BOOTSTRAP_DIMS=architecture ALEMBIC_TEST_RESCAN_DIMS=architecture npm --prefix AlembicTest run restart -- --project BiliDili --json --wait 12000 --no-preclean
curl -sS --max-time 5 http://127.0.0.1:50566/api/v1/modules/test-mode
curl -sS --max-time 10 -X POST -H 'Content-Type: application/json' --data '{"maxFiles":8,"contentMaxLines":40,"skipGuard":true}' http://127.0.0.1:50566/api/v1/jobs/bootstrap
node AlembicTest/scripts/probe-project-skill-delivery.mjs --project BiliDili --plugin AlembicPlugin --test-mode --bootstrap-dims architecture --rescan-dims architecture --timeout-ms 60000
curl -sS --max-time 5 http://127.0.0.1:50566/api/v1/jobs/bootstrap_mpjkpvdy_c12ba098/events?limit=500
curl -sS --max-time 5 http://127.0.0.1:50566/api/v1/modules/bootstrap/report/latest
git -C BiliDili status --short
```

## Evidence Files

- `AlembicTest/tmp/cssd-p4b-testmode-config-50566.json`
- `AlembicTest/tmp/cssd-p4b-health-50566.json`
- `AlembicTest/tmp/cssd-p4b-testmode-events-bootstrap_mpjkpvdy_c12ba098.json`
- `AlembicTest/tmp/cssd-p4b-testmode-report-latest-bootstrap_mpjkpvdy_c12ba098.json`
- `AlembicTest/tmp/project-skill-delivery-probe-2026-05-24T09-28-08-585Z.json`
- `${HOME}/.asd/workspaces/02a25032/.asd/logs/combined.log`
- `${HOME}/.asd/workspaces/02a25032/.asd/daemon.log`

## Failure Attribution

No blocking failure in the requested test scope.

Observations to route:

- Alembic / Dashboard: live REST events lagged until completion; after completion retained events were correct. If strict live append is required, investigate the job process event bridge flush/append cadence.
- Alembic: `ALEMBIC_TEST_MODE=1` with one dimension and `maxFiles=8` still took 417 seconds because producer budget remained high and generated 5 candidates. Consider a dedicated test-mode producer cap if the goal is fast smoke testing.
- AlembicPlugin: installed Codex plugin diagnostics still report runtime pin / metadata issues in the user-level cache, but the source/runtime artifact under test was at the expected fixed commits and the local Plugin route probe passed.

## Residual Risk

- This was a single-dimension smoke, not a full cold-start quality test.
- Alembic route runtime export was not completed automatically; it stayed pending for Plugin authorization by design.
- The probe validates runtime visibility through the Plugin MCP project-skill surface. A newly opened Codex thread may be needed to observe static project skill auto-discovery in the host UI skill list.

## Next Steps

- Accept `CSSD-P4B-TestMode-Test` as passing for the Plugin runtime delivery path and minimal Alembic route receipt.
- Add a small producer cap / smoke knob for future test-mode runs if under-one-minute validation is desired.
- Decide whether live REST event lag during an active job needs a follow-up task, since final retained events recovered correctly.
